# swagger 配置
swagger:
  enabled: true
  title: Msgbroker Swagger API
  gateway: http://${GATEWAY-HOST:msgbroker-gateway}:${GATEWAY-PORT:9999}
  token-url: ${swagger.gateway}/auth/oauth2/token
  scope: server
